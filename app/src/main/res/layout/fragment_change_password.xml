<?xml version="1.0" encoding="utf-8"?>
<ScrollView 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/scrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:scrollbars="none"
    android:overScrollMode="never"
    android:fitsSystemWindows="false"
    android:isScrollContainer="true"
    android:background="@color/background">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingHorizontal="16dp"
        android:paddingVertical="32dp"
        android:clipToPadding="false">

        <LinearLayout
            android:id="@+id/contentContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/titleChangePassword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/cambiar_contrasena_title"
                android:textSize="26sp"
                android:textColor="@color/text_primary"
                android:textAlignment="center"
                android:fontFamily="sans-serif"
                android:layout_marginBottom="32dp" />

            <com.example.alerti.CustomTextFieldView
                android:id="@+id/newPasswordField"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="44dp"
                android:layout_marginBottom="24dp"
                app:label="@string/nueva_contrasena_label"
                app:placeholder="@string/nueva_contrasena_placeholder"
                app:inputType="password" />

            <com.example.alerti.CustomTextFieldView
                android:id="@+id/confirmPasswordField"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="44dp"
                android:layout_marginBottom="48dp"
                app:label="@string/confirmar_contrasena_label"
                app:placeholder="@string/confirmar_contrasena_placeholder"
                app:inputType="password" />

            <LinearLayout
                android:id="@+id/buttonsContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="44dp"
                android:orientation="horizontal"
                android:gravity="center">

                <com.example.alerti.CustomButtonView
                    android:id="@+id/cancelButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="8dp"
                    app:buttonText="@string/cancelar_button"
                    app:buttonVariant="secondary"
                    app:buttonSize="small" />

                <com.example.alerti.CustomButtonView
                    android:id="@+id/changePasswordButton"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    app:buttonText="@string/cambiar_contrasena_button"
                    app:buttonVariant="primary"
                    app:buttonSize="small" />

            </LinearLayout>

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>